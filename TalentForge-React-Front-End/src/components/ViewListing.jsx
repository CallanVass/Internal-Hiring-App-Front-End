import React from 'react'

const ViewListing = ({ listing }) => {
    // const listing = { id, title, description, location, salary, datePosted, dateClosing, roleType, roleDuration, status, applicants }
  return (
    <div>ViewListing</div>
  )
}

export default ViewListing